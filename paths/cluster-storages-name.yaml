get:
  operationId: get_storage
  x-onedata-version: 3
  x-onedata-module: rest_service
  x-onedata-resource: storage
  tags:
    - Onepanel
  summary: Get storage details
  description: |
    Returns the details of a specific storage resource.
  produces:
    - application/json
  responses:
    '200':
      description: |
        Storage resource details.
      schema:
        $ref: '#/definitions/Storage'
    '400':
      description: Invalid request.
      schema:
        $ref: '#/definitions/Error'
    '403':
      description: Forbidden request.
      schema:
        $ref: '#/definitions/Error'
    '404':
      description: Oneprovider service configuration is not available. 
      schema:
        $ref: '#/definitions/Error'
    '500':
      description: Internal server error.
      schema:
        $ref: '#/definitions/Error'
  parameters:
  - name: name
    in: path
    type: string
    description: Name of the storage resource.
    required: true