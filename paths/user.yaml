get:
  operationId: get_current_user
  summary: Get details of authenticated user
  description: >
    Returns the details of the user performing the query.
  x-onedata-gri:
    type: onp_user
    aspect: current_user
  tags:
    - Current User
  produces:
    - application/json
  responses:
    '200':
      description: User details.
      schema:
        $ref: '#/definitions/CurrentUser'
    '401':
      description: Unauthorized request.
    '403':
      description: Forbidden request.
    '404':
      description: Request client is not a Onezone member.
    '500':
      description: Internal server error.
      schema:
        $ref: '#/definitions/Error'
  x-code-samples:
    - lang: Shell
      source: >
        onepanel-rest-cli getCurrentUser --host https://$PANEL_HOST:9443
        -H "x-auth-token: $TOKEN"
