get:
  operationId: get_ceph_monitor
  summary: Get Ceph monitor
  description: "" # @fixmme
  x-onedata-version: 3
  x-onedata-module: rest_ceph
  x-onedata-resource: service_ceph_monitor
  tags:
  - Oneprovider
  produces:
  - application/json
  responses:
    '200':
      description: "Ceph Monitor specification"
      schema:
        $ref: '#/definitions/CephMonitor'
    '401':
      description: Unauthorized request.
    '403':
      description: Forbidden request.
    '404':
      description: Ceph Monitor with given id does not exist.
    '500':
      description: Internal server error.
      schema:
        $ref: '#/definitions/ServiceError'
  parameters:
  - name: id
    description: Id of the Ceph Monitor to describe.
    in: path
    type: string
    required: true
  x-code-samples:
  - lang: Shell
    source: onepanel-rest-cli -u onepanel:$PASSPHRASE getCephMonitor --host https://$HOST:9443

delete:
  operationId: remove_ceph_monitor
  summary: Purge Ceph monitor.
  description: Removes the Ceph Monitor daemon.
  x-onedata-version: 3
  x-onedata-module: rest_ceph
  x-onedata-resource: service_ceph_monitor
  tags:
  - Oneprovider
  responses:
    '204':
      description: Monitor has been removed.
    '401':
      description: Unauthorized request.
    '403':
      description: Forbidden request.
    '404':
      description: Ceph Monitor with given id does not exist.
    '500':
      description: Internal server error.
      schema:
        $ref: '#/definitions/Error'
  parameters:
  - name: id
    description: Id of the Ceph Monitor to be removed.
    in: path
    type: string
    required: true
  x-code-samples:
  - lang: Shell
    source: onepanel-rest-cli -u onepanel:$PASSPHRASE removeCephMonitor --host https://$HOST:9443
