post:
  operationId: login
  summary: Create Onepanel user session
  description: Creates a new Onepanel user session.
  x-onedata-version: 3
  x-onedata-module: rest_onepanel_user
  x-onedata-resource: login
  tags:
    - Onepanel
  consumes:
    - application/json
  responses:
    '200':
      description: The created session details.
      schema:
        $ref: '#/definitions/SessionDetails'
    '400':
      description: Invalid request.
      schema:
        $ref: '#/definitions/Error'
    '403':
      description: Forbidden request.
    '500':
      description: Internal server error.
      schema:
        $ref: '#/definitions/Error'
  x-code-samples:
  - lang: Shell
    source: onepanel-rest-cli -u alice:secret login --host https://op.example.com:9443
