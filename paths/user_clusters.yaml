get:
  operationId: get_clusters
  summary: List user's clusters
  description: >
    Lists clusters to which current user belongs.
  x-onedata-version: 3
  x-onedata-module: rest_clusters
  x-onedata-resource: clusters
  tags:
    - Onepanel
  consumes:
    - application/json
  responses:
    '200':
      description: List of cluster ids.
      schema:
        $ref: '#/definitions/Ids'
    '400':
      description: Invalid request.
      schema:
        $ref: '#/definitions/Error'
    '403':
      description: Forbidden request.
    '404':
      description: Request client is not a Onezone member.
    '500':
      description: Internal server error.
      schema:
        $ref: '#/definitions/Error'
  x-code-samples:
    - lang: Shell
      source: onepanel-rest-cli -u onepanel:$PASSPHRASE getClusters --host https://$HOST:9443
