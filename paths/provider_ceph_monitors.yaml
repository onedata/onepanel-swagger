get:
  operationId: get_ceph_monitors
  summary: Get Ceph monitor
  description: "" # @fixmme
  x-onedata-version: 3
  x-onedata-module: rest_ceph
  x-onedata-resource: service_ceph_monitor
  tags:
  - Oneprovider
  produces:
  - application/json
  responses:
    '200':
      description: "" # @fixme
      schema:
        $ref: '#/definitions/CephMonitors'
    '401':
      description: Unauthorized request.
    '403':
      description: Forbidden request.
    '404':
      description: Ceph cluster not deployed.
    '500':
      description: Internal server error.
      schema:
        $ref: '#/definitions/ServiceError'
  x-code-samples:
  - lang: Shell
    source: onepanel-rest-cli -u alice:secret getCephMonitors --host https://op.example.com:9443


post:
  operationId: add_ceph_monitors
  summary: Add monitors to ceph cluster
  description: Deploys ceph monitor services on given hosts.
  x-onedata-version: 3
  x-onedata-module: rest_ceph
  x-onedata-resource: service_ceph_monitor
  tags:
  - Oneprovider
  consumes:
  - application/json
  responses:
    '204':
      description: Deployment process successfully started.
      headers:
        Location:
          type: string
          description: >
            The path to the task resource, which can be queried to check
            operation status.
    '400':
      description: Invalid request.
      schema:
        $ref: '#/definitions/Error'
    '401':
      description: Unauthorized request.
    '403':
      description: Forbidden request.
    '500':
      description: Internal server error.
      schema:
        $ref: '#/definitions/ServiceError'
  parameters:
  - name: serviceHosts
    in: body
    description: List of Ceph Monitor specifications.
    schema:
      $ref: '#/definitions/CephMonitors'
    required: true
  x-code-samples:
  - lang: Shell
    source: >
      echo '{"host": "n1.op.example.com"}' |
      onepanel-rest-cli -u alice:secret addCephMonitors --host https://op.example.com:9443

