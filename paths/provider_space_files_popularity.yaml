get:
  operationId: get_provider_space_files_popularity
  summary: Get config of space files popularity
  description: >
    Returns state and basic information about files popularity feature that
    can be turned on for the space
  x-onedata-version: 3
  x-onedata-module: rest_oneprovider
  x-onedata-resource: space_files_popularity
  tags:
    - Oneprovider
  produces:
    - application/json
  responses:
    '200':
      description: Information about files popularity feature for the space
      schema:
        $ref: '#/definitions/SpaceFilesPopularity'
    '401':
      description: Unauthorized request.
    '403':
      description: Forbidden request.
    '500':
      description: Internal server error.
      schema:
        $ref: '#/definitions/Error'
  parameters:
    - name: id
      in: path
      type: string
      description: The ID of a space
      required: true
patch:
  operationId: configure_space_files_popularity
  summary: Configure space files popularity
  description: Change files popularity feature options for the space
  x-onedata-version: 3
  x-onedata-module: rest_oneprovider
  x-onedata-resouce: space_files_popularity
  tags:
    - Oneprovider
  consumes:
    - application/json
  responses:
    '200':
      description: Information about files popularity feature for the space
      schema:
        $ref: '#/definitions/SpaceFilesPopularity'
    '400':
      description: Invalid request.
      schema:
        $ref: '#/definitions/Error'
    '401':
      description: Unauthorized request.
    '403':
      description: Forbidden request.
    '500':
      description: Internal server error.
      schema:
        $ref: '#/definitions/Error'
  parameters:
    - name: id
      in: path
      type: string
      description: The ID of a space
      required: true
