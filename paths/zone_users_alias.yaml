get:
  operationId: get_onezone_user
  summary: Get Onezone user details
  description: >
    Returns the configuration information of the Onezone user.
  x-onedata-version: 3
  x-onedata-module: rest_users
  x-onedata-resource: onezone_user
  tags:
    - Onezone
  produces:
    - application/json
  responses:
    '200':
      description: Oneozne user details.
      schema:
        $ref: '#/definitions/OnezoneUser'
    '401':
      description: Unauthorized request.
    '403':
      description: Forbidden request.
    '404':
      description: The user does not exist.
    '500':
      description: Internal server error.
      schema:
        $ref: '#/definitions/Error'
  parameters:
    - name: alias
      in: path
      description: Alias of the user to be described.
      type: string
      required: true
  x-code-samples:
    - lang: Shell
      source: onepanel-rest-cli -u alice:secret getOnezoneUser alias=bob
        --host https://$HOST:9443


patch:
  operationId: change_user_password
  summary: Set password for Onezone user
  description: >
    Sets new password for a Onezone user using Basic authentication.
  x-onedata-version: 3
  x-onedata-module: rest_users
  x-onedata-resource: onezone_user
  tags:
    - Onezone
  consumes:
    - application/json
  responses:
    '204':
      description: The user password has been successfully changed.
    '400':
      description: Invalid request.
      schema:
        $ref: '#/definitions/Error'
    '401':
      description: Unauthorized request.
    '403':
      description: Forbidden request.
    '404':
      description: The user does not exist.
    '500':
      description: Internal server error.
      schema:
        $ref: '#/definitions/Error'
  parameters:
    - name: alias
      in: path
      description: Alias of the user whose password is changed.
      type: string
      required: true
    - name: PasswordChangeRequest
      in: body
      schema:
        $ref: '#/definitions/PasswordChangeRequest'
      required: true
  x-code-samples:
    - lang: Shell
      source: onepanel-rest-cli -u admin:password changeUserPassword id=bob
        newPassword==new_bobs_password --host https://$HOST:9443
