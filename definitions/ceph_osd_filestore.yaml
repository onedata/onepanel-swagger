description: "@fixme"
allOf:
  - $ref: '#/definitions/CephOsd'
  - type: object
    x-discriminator-value: filestore
    properties:
      type:
        description: "@fixme"
        type: string
        enum: [filestore]
        x-discriminator-value: filestore
        x-discriminator: true
      path:
        type: string
        description: Relevant for plain filestore. If omitted, default location
          based on OSD id will be used.
    required:
      - type
