allOf:
  - $ref: '#/definitions/Configuration'
  - description: Oneprovider custom configuration.
  - properties:
      oneprovider:
        $ref: '#/definitions/Provider'
      onezone:
        type: object
        properties:
          domain_name:
            type: string
