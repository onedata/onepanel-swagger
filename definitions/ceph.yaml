allOf:
  - $ref: '#/definitions/Storage'
  - description: The Ceph storage configuration attributes.
  - properties:
      username:
        type: string
        description: The username for authentication to Ceph.
      key:
        type: string
        description: The key to access the Ceph resource.
      monitorHostname:
        type: string
        description: Monitor host name.
      clusterName:
        type: string
        description: Cluster name.
  - required:
    - name
    - username
    - key
    - monitorHostname
    - clusterName
example:
  type: CEPH
  username: user1
  key: LKJASHD6876ASDBJHV65765ASD
  monitorHostname: test.example.com
  clusterName: CephCluster1
