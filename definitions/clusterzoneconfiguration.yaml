type: object
description: Onezone cluster specification
properties:
  domainName:
    type: string
    description: Domain name of the service.
  nodes:
    type: object
    additionalProperties:
      type: object
      properties:
        hostname:
          type: string
          description: Node hostname
      required:
      - hostname
  managers:
    $ref: '#/definitions/ClusterManagers'
  workers:
    $ref: '#/definitions/ClusterWorkers'
  databases:
    $ref: '#/definitions/ClusterDatabases'
required:
- domainName
- nodes
- managers
- workers
- databases