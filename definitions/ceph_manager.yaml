type: object
description: Ceph manager specification.
properties:
  host:
    type: string
    description: Host on which given Manager should be deployed.
  id:
    type: string
    description: Manager identifier. By default the hostname is used.