type: object
description: Details of a onepanel node.
properties:
  hostname:
    type: string
    description: Hostname of the node.
  application:
    type: string
    description: Type of application managed by this onepanel.
    enum: [oneprovider, onezone]
required:
- hostname
- application
