type: object
description: Details of a onepanel node.
properties:
  hostname:
    type: string
    description: Erlang hostname of the node.
  application:
    type: string
    description: Type of application managed by this onepanel.
    enum: [oneprovider, onezone]
required:
- hostname
- application

