type: object
description: Describes a the properties of a Worker process instance.
properties:
  clusterDomain:
    type: string
    description: ???
  onezoneDomain:
    type: string
    description: ???
  cookie:
    type: string
    description: Authentication cookie.
  openedFilesLimit:
    type: integer
    description: The limit of files that this worker process can have opened simultanously.
  processLimit:
    type: integer
    description: The number of processes to which each worker can span on a single host.
required:
  - clusterDomain
  - onezoneDomain