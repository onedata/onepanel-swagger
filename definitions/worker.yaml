type: object
properties:
  id:
    type: string
    description: Unique worker ID
  name:
    type: string
    description: The name of the worker - can be the same as the hostname.
  running:
    type: boolean
    description: This flag determines whether the worker should be running or should be stopped. It does not necessarily reflect the actual state of the worker. 