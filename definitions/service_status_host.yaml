type: object
description: The service status.
properties:
  status:
    type: string
    description: The service status.
    enum:
      - healthy
      - unhealthy
      - stopped
      - missing
required:
  - status