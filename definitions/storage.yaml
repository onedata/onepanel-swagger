type: object
description: Describes the properties of a storage resource.
discriminator: type
properties:
  type:
    type: string
    description: Type of storage resource
    enum:
      - POSIX
      - S3
      - CEPH
required:
  - type
