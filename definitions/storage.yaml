type: object
description: Describes the properties of a storage resource.
discriminator: type
x-erlang-oneof: true
x-erlang-subclasses:
  - ceph
  - s3
  - posix
properties:
  type:
    type: string
    description: Type of storage resource
    enum:
      - POSIX
      - S3
      - CEPH
required:
  - type
  - mountPoint
