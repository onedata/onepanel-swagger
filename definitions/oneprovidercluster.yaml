allOf:
  - $ref: '#/definitions/Cluster'
  - description: Oneprovider cluster specific configuration.
  - properties:
      storages:
        type: object
        additionalProperties:
          $ref: '#/definitions/Storage'
