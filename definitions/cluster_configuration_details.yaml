type: object
description: The cluster configuration.
properties:
  master:
    type: string
    description: Host designated for managing service deployment and subsequent starts.
  databases:
    $ref: '#/definitions/DatabaseHosts'
  managers:
    $ref: '#/definitions/ManagerHosts'
  workers:
    $ref: '#/definitions/WorkerHosts'
required:
  - master
  - databases
  - managers
  - workers
