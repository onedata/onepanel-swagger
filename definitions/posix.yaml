allOf:
  - $ref: '#/definitions/Storage'
  - x-discriminator-value: "POSIX"
  - description: POSIX specific storage configuration attributes.
  - properties:
      mountPoint:
        type: string
        description: |
          The absolute path of the mountpoint where the storage is mounted 
          on the Oneprovider nodes.
  - required:
    - mountPoint
example:
  type: POSIX
  mountPoint: /mnt/nas/volume1
